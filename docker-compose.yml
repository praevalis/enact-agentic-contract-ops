services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${ENACT_DATABASE_NAME:-enact}
      POSTGRES_USER: ${POSTGRES_USER:-enact_bootstrap}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-local-bootstrap-only}
      ENACT_MIGRATE_PASSWORD: ${ENACT_MIGRATE_PASSWORD:-local-migrate-only}
      ENACT_APP_PASSWORD: ${ENACT_DATABASE_PASSWORD:-local-app-only}
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./infra/postgres/bootstrap:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

  object-store:
    image: quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-enact-local}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-enact-local-secret}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - object-store-data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 5s
      retries: 10

  object-store-init:
    image: quay.io/minio/mc:RELEASE.2025-08-13T08-35-41Z
    entrypoint:
      - /bin/sh
      - -c
      - >-
        mc alias set local http://object-store:9000 "$${MINIO_ROOT_USER}"
        "$${MINIO_ROOT_PASSWORD}" && mc mb --ignore-existing
        "local/$${ENACT_OBJECT_STORAGE_BUCKET}"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-enact-local}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-enact-local-secret}
      ENACT_OBJECT_STORAGE_BUCKET: ${ENACT_OBJECT_STORAGE_BUCKET:-enact-documents}
    depends_on:
      object-store:
        condition: service_healthy
    restart: "no"

  migrate:
    build:
      context: .
      dockerfile: infra/docker/server.Dockerfile
    command:
      [
        "alembic",
        "-c",
        "apps/server/alembic.ini",
        "upgrade",
        "head",
      ]
    environment:
      ENACT_DATABASE_HOST: postgres
      ENACT_DATABASE_USERNAME: enact_migrate
      ENACT_DATABASE_PASSWORD: ${ENACT_MIGRATE_PASSWORD:-local-migrate-only}
      ENACT_DATABASE_PORT: 5432
      ENACT_DATABASE_NAME: ${ENACT_DATABASE_NAME:-enact}
      ENACT_DATABASE_SSL_MODE: ${ENACT_DATABASE_SSL_MODE:-disable}
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  api:
    build:
      context: .
      dockerfile: infra/docker/server.Dockerfile
    environment:
      ENACT_ENVIRONMENT: ${ENACT_ENVIRONMENT:-development}
      ENACT_LOG_LEVEL: ${ENACT_LOG_LEVEL:-INFO}
      ENACT_DATABASE_HOST: postgres
      ENACT_DATABASE_USERNAME: enact_app
      ENACT_DATABASE_PASSWORD: ${ENACT_DATABASE_PASSWORD:-local-app-only}
      ENACT_DATABASE_PORT: 5432
      ENACT_DATABASE_NAME: ${ENACT_DATABASE_NAME:-enact}
      ENACT_DATABASE_SSL_MODE: ${ENACT_DATABASE_SSL_MODE:-disable}
      ENACT_API_URL: ${ENACT_API_URL:-http://localhost:8000}
      ENACT_OBJECT_STORAGE_ENDPOINT_URL: http://object-store:9000
      ENACT_OBJECT_STORAGE_ACCESS_KEY: ${MINIO_ROOT_USER:-enact-local}
      ENACT_OBJECT_STORAGE_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-enact-local-secret}
      ENACT_OBJECT_STORAGE_BUCKET: ${ENACT_OBJECT_STORAGE_BUCKET:-enact-documents}
      ENACT_OBJECT_STORAGE_REGION: ${ENACT_OBJECT_STORAGE_REGION:-us-east-1}
    restart: unless-stopped
    ports:
      - "8000:8000"
    depends_on:
      migrate:
        condition: service_completed_successfully
      object-store-init:
        condition: service_completed_successfully

  web:
    build:
      context: .
      dockerfile: infra/docker/web.Dockerfile
      args:
        VITE_API_BASE_URL: http://localhost:8000/api
    restart: unless-stopped
    ports:
      - "5173:80"
    depends_on:
      api:
        condition: service_started

volumes:
  postgres-data:
  object-store-data:
